package dev.slne.surf.combat.gradle

import org.gradle.api.Plugin
import org.gradle.api.initialization.Settings

class IncludeCombatModuleSettingsPlugin : Plugin<Settings> {
    override fun apply(target: Settings) {

    }
}